{
  "name": "pgvector-starter",
  "version": "1.0.0",
  "private": true,
  "description": "Semantic vector search on Dockhold's managed Postgres (pgvector) — zero config.",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.0",
    "express-rate-limit": "^7.4.0",
    "pg": "^8.13.0"
  }
}
